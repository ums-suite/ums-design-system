---
'@ums/design-system': minor
---

Initial design-token pipeline (color, typography, spacing, radius, elevation, motion,
breakpoints), the theming/motion engine, the Phosphor icon registry, the brand asset registry,
and the Button component family (Button, Icon Button, FAB, Split Button). All additive per
requirement-spec.md §9 ("non-breaking additions... ship as a minor version") -- no existing
token or component API is changed by this release.
